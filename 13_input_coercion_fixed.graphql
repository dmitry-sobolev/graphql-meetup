directive @model(inputName: String) on INPUT_OBJECT | OBJECT

input Coordinates @model(inputName: "Coordinates") {
    lat: Float!
    lng: Float!
}

input MapObject @model(inputName: "MapObject") {
    name: String!
    coordinates: Coordinates!
}

type Mutation {
    createMapObject(new: MapObject!): Boolean!
}
