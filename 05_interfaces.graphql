interface IPet {
    id: ID!
    name: String!
}

type Cat implements IPet {
    id: ID!
    name: String!
    meowVolume: Int!
}


type Pigeon implements IPet {
    id: ID!
    name: String!
    isWhite: Boolean!
}